import { ChildHostDirective } from './child-host.directive';

describe('ChildHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildHostDirective();
    expect(directive).toBeTruthy();
  });
});
